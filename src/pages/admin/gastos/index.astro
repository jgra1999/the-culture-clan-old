---
export const prerender = false

import AdminLayout from '@/layouts/AdminLayout.astro'
import { DataTable } from '@/components/admin/tables/DataTable'

const cookie = Astro.cookies.get('admin')

if (cookie && cookie.value !== 'true') {
	return Astro.redirect('/admin/login')
}

const headers = ['Descripción', 'Cantidad', 'Fecha', 'Accion']
---

<AdminLayout title='Inversiones/Gastos'>
	<div class='overflow-x-auto shadow-md sm:rounded-lg my-8'>
		<DataTable dbTableName='investments' client:load />
	</div>
</AdminLayout>
